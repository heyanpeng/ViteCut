# Docker Compose 环境变量模板
# 复制为 .env 并填写实际值后执行: docker compose build && docker compose up -d

# MySQL（必填）
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=vitecut

# 前端构建变量（可选，不填则素材搜索不可用）
# VITE_PEXELS_API_KEY=    # Pexels 视频/图片素材
# VITE_FREESOUND_API_KEY= # Freesound 音效素材
