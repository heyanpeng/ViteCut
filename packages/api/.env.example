# 环境变量模板 - 线下用 .env.development，线上用 .env.production 或系统变量
# 复制为 .env.development（线下）或 .env.production（线上）并填写实际值
# 云数据库（如阿里云 RDS）可能需手动建库：CREATE DATABASE IF NOT EXISTS vitecut;

MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=vitecut
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=vitecut

PORT=3001

# 线上部署时填写，用于生成媒体/输出文件的 URL（如 https://yourdomain.com）
# 不填则从请求头推断，本地开发会得到 localhost
# PUBLIC_BASE_URL=https://yourdomain.com
